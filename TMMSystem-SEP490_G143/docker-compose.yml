version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: tmms-mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: tmms123456
      MYSQL_DATABASE: tmms_db
      MYSQL_USER: tmms_user
      MYSQL_PASSWORD: tmms123456
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    command: --default-authentication-plugin=mysql_native_password
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      timeout: 20s
      retries: 10

volumes:
  mysql_data:
    driver: local